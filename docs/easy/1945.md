# 1945. Sum of Digits of String After Convert

## Question
You are given a string `s` consisting of lowercase English letters, and an integer `k`. Your task is to *convert* the string into an integer by a special process, and then *transform* it by summing its digits repeatedly `k` times. More specifically, perform the following steps:

1. **Convert** `s` into an integer by replacing each letter with its position in the alphabet (i.e. replace `'a'` with `1`, `'b'` with `2`, ..., `'z'` with `26`).
2. **Transform** the integer by replacing it with the **sum of its digits**.
3. Repeat the **transform** operation (step 2) `k` **times** in total.

For example, if `s = "zbax"` and `k = 2`, then the resulting integer would be `8` by the following operations:

1. **Convert**: `"zbax" ➝ "(26)(2)(1)(24)" ➝ "262124" ➝ 262124`
2. **Transform #1**: `262124 ➝ 2 + 6 + 2 + 1 + 2 + 4 ➝ 17`
3. **Transform #2**: `17 ➝ 1 + 7 ➝ 8`

Return the **resulting** **integer** after performing the **operations** described above.

### Example 1:
```
Input: s = "iiii", k = 1
Output: 36

Explanation:
The operations are as follows:
- Convert: "iiii" ➝ "(9)(9)(9)(9)" ➝ "9999" ➝ 9999
- Transform #1: 9999 ➝ 9 + 9 + 9 + 9 ➝ 36

Thus the resulting integer is 36.
```

### Example 2:
```
Input: s = "leetcode", k = 2
Output: 6

Explanation:
The operations are as follows:
- Convert: "leetcode" ➝ "(12)(5)(5)(20)(3)(15)(4)(5)" ➝ "12552031545" ➝ 12552031545
- Transform #1: 12552031545 ➝ 1 + 2 + 5 + 5 + 2 + 0 + 3 + 1 + 5 + 4 + 5 ➝ 33
- Transform #2: 33 ➝ 3 + 3 ➝ 6

Thus the resulting integer is 6.
```

### Example 3:
```
Input: s = "zbax", k = 2
Output: 8
```

### Constraints:
- `1 <= s.length <= 100`
- `1 <= k <= 10`
- `s` consists of lowercase English letters.

## Solution
```typescript
function getLucky(s: string, k: number): number {
    let arr: number[] = [];

    for (let i = 0; i < s.length; i++) {
        let num = s.charCodeAt(i) - 96;
        if (num > 9) {
            arr.push(parseInt((num / 10).toString()));
        }
        arr.push(num % 10);
    }

    if (arr.length === 1) {
        return arr[0];
    }

    let result = arr.reduce((a, c) => a + c, 0);

    for (let i = 1; i < k; i++) {
        if (result <= 9) {
            break;
        }

        let save = result;
        result = 0;
        do {
            result += save % 10;
            save = parseInt((save / 10).toString());
        } while (save > 0);
    }

    return result;
};
```

## Mindset
1. For each character, get its ASCII code. Since `a` should map to 1 and the ASCII code of `a` is 97, subtracting 96 converts a lowercase letter to 1–26.
2. If the converted value is greater than or equal to 10 (i.e., `j` to `z`), split it into individual digits.
3. Store the digits from steps (1) and (2) in an array.
4. Iterate `k` times to sum the digits. After the first iteration, the result is a single number; if it is greater than or equal to 10, split it into digits and sum again.
5. If the array contains only one element and it is less than or equal to 9, return the result early.